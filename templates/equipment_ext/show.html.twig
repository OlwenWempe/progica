{% extends 'base.html.twig' %}

{% block title %}EquipmentExt{% endblock %}

{% block body %}
    <h1>EquipmentExt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment_ext.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipment_ext.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipment_ext_index') }}">back to list</a>

    <a href="{{ path('app_equipment_ext_edit', {'id': equipment_ext.id}) }}">edit</a>

    {{ include('equipment_ext/_delete_form.html.twig') }}
{% endblock %}
