{% extends 'base.html.twig' %}

{% block title %}EquipmentInt{% endblock %}

{% block body %}
    <h1>EquipmentInt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ equipment_int.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ equipment_int.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_equipment_int_index') }}">back to list</a>

    <a href="{{ path('app_equipment_int_edit', {'id': equipment_int.id}) }}">edit</a>

    {{ include('equipment_int/_delete_form.html.twig') }}
{% endblock %}
