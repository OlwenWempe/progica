<form method="post" action="{{ path('app_equipment_int_delete', {'id': equipment_int.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ equipment_int.id) }}">
    <button class="btn">Delete</button>
</form>
